[project]
name = "needle_mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "cactus-needle>=2.0.8",
    "mcp>=2.0.0",
    "json-repair",
]

[project.scripts]
needle = "needle_mcp.mcp:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
